/**
 * App.tsx is no longer the entry point.
 * Routing is handled by routes.tsx + ProtectedRoute.
 * This file is kept for backwards compatibility only.
 */
export default function App() {
  return null
}
